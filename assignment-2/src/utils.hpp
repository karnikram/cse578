#pragma once

#include <iostream>
#include <string>
#include <Eigen/Core>
#include <fstream>
#include <random>
#include <algorithm>

namespace utils
{
	std::vector<int> generateRandomVector(const int &lrange, const int &urange, const int &size);
}

